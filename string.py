# Indexing
cake = "vanilla"
print(cake[0])
food = "I love paneer ke parathe"
for character in food:
    print(character) # prints the characters one by one.
for character in food:
    print(food) # prints the whole string as many times as the character in the string.
